# Airlink v3.0 — RADIUS clients (NAS). For the dev proof we trust localhost
# (radtest inside the container) and the Docker bridge network. Real MikroTik
# NAS devices are added here (or in the `nas` table) in production.
client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
}

client docker_net {
    ipaddr = 172.16.0.0/12
    secret = testing123
}
